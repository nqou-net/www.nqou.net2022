desc 'deploy';
task deploy => sub {
    sh qw{hexo clean};
    sh qw{tools/rename_posts};
    sh qw{hexo deploy};
};

desc 'new post';
task new => sub {
    sh qw{hexo new post};
};

task default => sub { sh q{daiku -T} };
